"""Shared core for the thichhoc.com dictionary program.

Everything here is language-pair agnostic: the entry schema, the HTML
renderer and the three device builders are used unchanged by both
``dict-en-vi`` (Phase A) and ``dict-zh-vi`` (Phase B).
"""

__version__ = "0.1.0"
